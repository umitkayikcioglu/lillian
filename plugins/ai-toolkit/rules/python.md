---
trigger: glob
globs: "**/*.py, **/pyproject.toml, **/setup.py, **/setup.cfg, **/requirements*.txt, **/Pipfile, **/Pipfile.lock, **/poetry.lock, **/uv.lock, **/.python-version, **/tox.ini, **/noxfile.py"
---

# Python Instructions

Use `../skills/python-development/SKILL.md` for Python application, API, CLI, worker, library, and packaging work.

Resolve project ownership, interpreter, environment, package manager, framework, and validation commands from repository evidence. Python extensions and conventional directories are routing hints only; do not activate a framework, test runner, or tool from filenames alone.

Preserve the owning project boundary in every validation result. Use `../skills/web-frontend-development/references/validation-output.md` for result fields and statuses. Report missing, conflicting, or ambiguous evidence explicitly instead of guessing a command.
